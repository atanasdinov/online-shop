package com.scalefocus.shop.enumeration;

import com.scalefocus.shop.model.entity.Role;

/**
 * <b>This enum contains the different {@link Role} types.</b>
 */
public enum RoleType {
    ADMIN,
    MODERATOR,
    USER
}
